# W10 Kinematics Configuration
# Parameters for inverse kinematics solver

# Solver parameters
ik_solver:
  max_iterations: 1000
  tolerance: 1e-6
  step_size: 0.01

# Arm angle dimension reduction parameters
arm_angle_reduction:
  enabled: true
  # Number of primary DOFs to keep
  primary_dofs: 3
